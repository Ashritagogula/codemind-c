#include<stdio.h>
#include<string.h>
int main(){
    char s[10001];
    scanf("%s", s);
    int count = 1;
    for(int i = 0; s[i] != '\0'; i++){
        if(s[i] >= 'A' && s[i] <= 'Z')
            count++;
    }
    printf("%d\n", count);
    return 0;
}
